# SQLD 2025년 기출 복원 기반 전체 세트

> 공개된 기출 복원 자료의 출제 주제와 경향을 참고해 새로 구성한 학습용 문제입니다. K-DATA 공식 기출 원문이나 특정 회차의 완전한 복제본이 아닙니다.

- 참고 회차: 56~59회
- 시험 구성: 50문항 · 90분
- 1과목 데이터 모델링의 이해: 10문항
- 2과목 SQL 기본 및 활용: 40문항
- 연도별 빈출 포인트: 3층 스키마 · 엔터티 · ACID · 셀프 조인 · 계층형

## 문제

### 1과목 · 데이터 모델링의 이해

#### 1. [난이도 중] 다음 중 'Phantom Read'에 대한 설명으로 가장 적절한 것은?

1. 모든 결정자가 후보키인 상태로 3NF를 강화한 정규형
2. 같은 조건으로 재조회 시 다른 트랜잭션의 삽입으로 새 행이 나타나는 현상
3. 상대 엔터티 인스턴스 없이도 존재할 수 있는 관계
4. X의 값이 정해지면 Y의 값이 하나로 결정되는 관계(X→Y)
5. 1NF를 만족하며 부분 함수 종속을 제거한 상태

#### 2. [난이도 중] 다음 중 '행위 엔터티'에 대한 설명으로 가장 적절한 것은?

1. 모델링 과정에서 코드나 일련번호 등을 새로 만든 속성
2. 한 속성이 가질 수 있는 값의 범위와 제약
3. 관계 참여가 필수인지 선택인지를 나타내는 성질
4. 두 개 이상의 엔터티 사이의 행위로 발생하는 엔터티
5. 인스턴스가 가지는 더 이상 분리할 수 없는 최소 의미 단위

#### 3. [난이도 중] "약속된 표기법으로 누구나 쉽게 이해할 수 있도록 표현하는 것"에 해당하는 개념은?

1. 물리적 독립성
2. 기본 엔터티
3. 단순화
4. 논리적 모델링
5. 개념 스키마

#### 4. [난이도 하] "업무의 핵심 엔터티와 관계를 추상적으로 도출하는 단계"에 해당하는 개념은?

1. 개념적 모델링
2. 외부 스키마
3. 내부 스키마
4. 엔터티
5. 중심 엔터티

#### 5. [난이도 하] 다음 중 '정규화'에 대한 설명으로 가장 적절한 것은?

1. 데이터 중복을 줄이고 이상현상을 방지하기 위해 엔터티를 분해하는 과정
2. X의 값이 정해지면 Y의 값이 하나로 결정되는 관계(X→Y)
3. 1NF를 만족하며 부분 함수 종속을 제거한 상태
4. 모든 결정자가 후보키인 상태로 3NF를 강화한 정규형
5. 중복된 데이터 중 일부만 수정하면 불일치가 발생하는 현상

#### 6. [난이도 상] "식별자·속성·정규화를 통해 재사용성 높은 데이터 구조를 설계하는 단계"에 해당하는 개념은?

1. 논리적 독립성
2. 기본 엔터티
3. 행위 엔터티
4. 논리적 모델링
5. 개념 스키마

#### 7. [난이도 하] 다음 중 '속성'에 대한 설명으로 가장 적절한 것은?

1. 1:1, 1:M처럼 관계에 참여하는 인스턴스 수의 대응 비율
2. 엔터티에서 인스턴스를 유일하게 구분하는 대표 식별자
3. 인스턴스가 가지는 더 이상 분리할 수 없는 최소 의미 단위
4. 업무로부터 직접 수집되어 원래부터 존재하는 속성
5. 다른 속성의 값으로 계산되어 만들어지는 속성

#### 8. [난이도 중] 다음 중 '관계 차수'에 대한 설명으로 가장 적절한 것은?

1. 부모 식별자가 자식의 주식별자 구성에 포함되는 관계
2. 현실 세계를 일정한 형식에 맞춰 간략하게 표현하는 것
3. 1:1, 1:M처럼 관계에 참여하는 인스턴스 수의 대응 비율
4. 관계 참여가 필수인지 선택인지를 나타내는 성질
5. 유일하게 구분할 수 있지만 대표성을 갖지 못한 식별자

#### 9. [난이도 하] "기본 엔터티로부터 파생되어 업무의 중심 역할을 하는 엔터티"에 해당하는 개념은?

1. 관계 차수
2. 주식별자
3. 중심 엔터티
4. 속성
5. 설계 속성

#### 10. [난이도 상] "두 개 이상의 엔터티 사이의 행위로 발생하는 엔터티"에 해당하는 개념은?

1. 행위 엔터티
2. 기본 속성
3. 파생 속성
4. 관계 선택성
5. 보조식별자

### 2과목 · SQL 기본 및 활용

#### 11. [난이도 하] 다음 중 'RANK'에 대한 설명으로 가장 적절한 것은?

1. 동점에 같은 순위를 주고 다음 순위를 건너뛰는 윈도우 함수
2. 동점에 같은 순위를 주고 다음 순위를 연속으로 부여하는 윈도우 함수
3. 현재 행을 기준으로 이후 행의 값을 참조하는 윈도우 함수
4. 전체 행을 지정한 N개 그룹으로 균등 분할하여 그룹 번호를 부여하는 함수
5. Oracle 계층형 질의에서 부모와 자식의 연결 방향을 정의하는 절

#### 12. [난이도 중] "Oracle 계층형 질의에서 부모와 자식의 연결 방향을 정의하는 절"에 해당하는 개념은?

1. CONNECT BY PRIOR
2. REGEXP_SUBSTR
3. 인라인 뷰
4. EXISTS
5. ALL

#### 13. [난이도 상] 다음 중 'CONNECT BY PRIOR'에 대한 설명으로 가장 적절한 것은?

1. SELECT 절에서 각 행마다 최대 1행 1컬럼을 반환하는 서브쿼리
2. 내부 쿼리가 외부 쿼리의 현재 행 값을 참조하여 반복 실행되는 서브쿼리
3. 서브쿼리 결과 행의 존재 여부만 판단하는 연산자
4. Oracle 계층형 질의에서 부모와 자식의 연결 방향을 정의하는 절
5. 행으로 나열된 범주 값을 열로 펼쳐 집계하는 구문

#### 14. [난이도 하] 다음 중 'LAG'에 대한 설명으로 가장 적절한 것은?

1. 전체 행을 지정한 N개 그룹으로 균등 분할하여 그룹 번호를 부여하는 함수
2. Oracle 계층형 질의에서 부모와 자식의 연결 방향을 정의하는 절
3. 정규표현식에 일치하는 부분 문자열을 추출하는 함수
4. 현재 행을 기준으로 이전 행의 값을 참조하는 윈도우 함수
5. 현재 행을 기준으로 이후 행의 값을 참조하는 윈도우 함수

#### 15. [난이도 상] 다음 중 'ROWNUM'에 대한 설명으로 가장 적절한 것은?

1. Oracle 계층형 질의에서 부모와 자식의 연결 방향을 정의하는 절
2. 정규표현식에 일치하는 부분 문자열을 추출하는 함수
3. FROM 절에서 임시 테이블처럼 사용되는 서브쿼리
4. 서브쿼리 결과 목록 중 하나와 같으면 TRUE를 반환하는 연산자
5. Oracle에서 결과가 반환되는 과정에 임시로 부여되는 순번

#### 16. [난이도 중] 다음 중 'FIRST_VALUE'에 대한 설명으로 가장 적절한 것은?

1. FROM 절에서 임시 테이블처럼 사용되는 서브쿼리
2. 윈도우 프레임에서 정렬 순서상 첫 번째 값을 반환하는 함수
3. 전체 행을 지정한 N개 그룹으로 균등 분할하여 그룹 번호를 부여하는 함수
4. Oracle 계층형 질의에서 부모와 자식의 연결 방향을 정의하는 절
5. 정규표현식에 일치하는 부분 문자열을 추출하는 함수

#### 17. [난이도 상] "트랜잭션 전후로 데이터베이스가 무결성 규칙을 유지하는 성질"에 해당하는 개념은?

1. ON DELETE CASCADE
2. 일관성
3. 영속성
4. ALTER
5. UNIQUE

#### 18. [난이도 하] 다음 중 'DELETE'에 대한 설명으로 가장 적절한 것은?

1. 트랜잭션 작업이 모두 성공하거나 모두 실패해야 하는 성질
2. 동시에 실행되는 트랜잭션이 서로 간섭하지 않도록 보장하는 성질
3. 테이블에서 조건에 맞는 행을 제거하는 DML 명령어
4. 매칭 여부에 따라 INSERT·UPDATE·DELETE를 조건부 수행하는 DML
5. 아직 확정하지 않은 트랜잭션의 변경을 취소하는 TCL 명령어

#### 19. [난이도 중] "시스템 권한이나 ROLE을 다른 사용자에게 재부여할 수 있게 하는 옵션"에 해당하는 개념은?

1. WITH ADMIN OPTION
2. INSERT
3. DELETE
4. ROLLBACK
5. 원자성

#### 20. [난이도 하] "행으로 나열된 범주 값을 열로 펼쳐 집계하는 구문"에 해당하는 개념은?

1. 상관 서브쿼리
2. ANY/SOME
3. UNION
4. PIVOT
5. 스칼라 서브쿼리

#### 21. [난이도 중] "GROUP BY로 만들어진 그룹에 조건을 적용하는 절"에 해당하는 개념은?

1. HAVING
2. NVL
3. NULLIF
4. 집계 함수
5. LEFT OUTER JOIN

#### 22. [난이도 하] 다음 중 'DML'에 대한 설명으로 가장 적절한 것은?

1. GRANT, REVOKE처럼 권한을 부여하거나 회수하는 언어
2. 선택한 컬럼 조합이 같은 중복 행을 하나로 축약하는 키워드
3. GROUP BY보다 먼저 개별 행을 필터링하는 절
4. SELECT 별칭과 위치 번호를 사용할 수 있는 정렬 절
5. SELECT, INSERT, UPDATE, DELETE처럼 행 데이터를 조작하는 언어

#### 23. [난이도 하] 다음 중 'DCL'에 대한 설명으로 가장 적절한 것은?

1. SELECT 문의 논리적 처리 순서
2. GROUP BY로 만들어진 그룹에 조건을 적용하는 절
3. Oracle에서 NULL이면 지정한 대체값을 반환하는 함수
4. GRANT, REVOKE처럼 권한을 부여하거나 회수하는 언어
5. COMMIT, ROLLBACK, SAVEPOINT처럼 트랜잭션을 제어하는 언어

#### 24. [난이도 중] 다음 중 'FULL OUTER JOIN'에 대한 설명으로 가장 적절한 것은?

1. 같은 테이블을 별칭으로 구분하여 자기 자신과 조인하는 방식
2. = 이외의 비교 연산자(BETWEEN 등)로 범위를 연결하는 조인
3. 조건에 따라 값을 선택하며 ELSE가 없고 모든 조건이 거짓이면 NULL을 반환하는 표현식
4. 양쪽 테이블 모두의 불일치 행을 보존하는 조인
5. 두 테이블에서 이름이 같은 모든 컬럼을 자동으로 조인하는 방식

#### 25. [난이도 하] "양쪽 테이블 모두의 불일치 행을 보존하는 조인"에 해당하는 개념은?

1. FULL OUTER JOIN
2. CROSS JOIN
3. USING 절
4. CASE
5. DML

#### 26. [난이도 중] "컬럼 값의 중복을 금지하지만 NULL은 허용하는 제약조건"에 해당하는 개념은?

1. DML
2. UPDATE
3. UNIQUE
4. ON DELETE CASCADE
5. SEQUENCE

#### 27. [난이도 하] "같은 테이블을 별칭으로 구분하여 자기 자신과 조인하는 방식"에 해당하는 개념은?

1. 비등가 조인
2. CASE
3. DCL
4. SELECT DISTINCT
5. SELF JOIN

#### 28. [난이도 중] "기존 객체의 구조를 변경(컬럼 추가·수정·삭제 등)하는 DDL 명령어"에 해당하는 개념은?

1. FOREIGN KEY
2. SEQUENCE
3. WITH ADMIN OPTION
4. ALTER
5. PRIMARY KEY

#### 29. [난이도 중] 다음 중 '영속성'에 대한 설명으로 가장 적절한 것은?

1. 테이블·뷰·시퀀스 등 객체 구조를 새로 생성하는 DDL 명령어
2. 테이블 구조를 남기고 모든 행을 빠르게 제거하는 DDL 명령어
3. 컬럼 값의 중복을 금지하지만 NULL은 허용하는 제약조건
4. 부모 행 삭제 시 참조하는 자식 행도 함께 삭제하는 옵션
5. COMMIT된 트랜잭션의 결과가 장애 후에도 영구 보존되는 성질

#### 30. [난이도 중] 다음 중 'ROW_NUMBER'에 대한 설명으로 가장 적절한 것은?

1. Oracle에서 결과가 반환되는 과정에 임시로 부여되는 순번
2. 동점 여부와 무관하게 각 행에 고유한 순번을 부여하는 윈도우 함수
3. 동점에 같은 순위를 주고 다음 순위를 건너뛰는 윈도우 함수
4. 현재 행을 기준으로 이전 행의 값을 참조하는 윈도우 함수
5. 윈도우 프레임에서 정렬 순서상 첫 번째 값을 반환하는 함수

#### 31. [난이도 상] 다음 중 'WITH ADMIN OPTION'에 대한 설명으로 가장 적절한 것은?

1. 기존 행의 컬럼 값을 SET 절로 변경하는 DML 명령어
2. 매칭 여부에 따라 INSERT·UPDATE·DELETE를 조건부 수행하는 DML
3. 아직 확정하지 않은 트랜잭션의 변경을 취소하는 TCL 명령어
4. 시스템 권한이나 ROLE을 다른 사용자에게 재부여할 수 있게 하는 옵션
5. INSERT·UPDATE·DELETE·MERGE처럼 행 데이터를 조작하는 언어

#### 32. [난이도 중] 다음 중 'CASE'에 대한 설명으로 가장 적절한 것은?

1. GROUP BY보다 먼저 개별 행을 필터링하는 절
2. 조건에 따라 값을 선택하며 ELSE가 없고 모든 조건이 거짓이면 NULL을 반환하는 표현식
3. CREATE, ALTER, DROP, RENAME, TRUNCATE처럼 객체 구조를 정의하는 언어
4. GRANT, REVOKE처럼 권한을 부여하거나 회수하는 언어
5. 선택한 컬럼 조합이 같은 중복 행을 하나로 축약하는 키워드

#### 33. [난이도 상] "테이블에서 조건에 맞는 행을 제거하는 DML 명령어"에 해당하는 개념은?

1. SAVEPOINT
2. 고립성
3. CREATE
4. DELETE
5. COMMIT

#### 34. [난이도 중] "테이블에 새 행을 추가하는 DML 명령어"에 해당하는 개념은?

1. 원자성
2. 고립성
3. INSERT
4. DELETE
5. COMMIT

#### 35. [난이도 중] "동점에 같은 순위를 주고 다음 순위를 연속으로 부여하는 윈도우 함수"에 해당하는 개념은?

1. PIVOT
2. 스칼라 서브쿼리
3. DENSE_RANK
4. LEAD
5. NTILE

#### 36. [난이도 하] 다음 중 'COMMIT'에 대한 설명으로 가장 적절한 것은?

1. 현재 트랜잭션의 변경을 영구적으로 확정하는 TCL 명령어
2. 아직 확정하지 않은 트랜잭션의 변경을 취소하는 TCL 명령어
3. 트랜잭션 작업이 모두 성공하거나 모두 실패해야 하는 성질
4. 동시에 실행되는 트랜잭션이 서로 간섭하지 않도록 보장하는 성질
5. 테이블·뷰·시퀀스 등 객체 구조를 새로 생성하는 DDL 명령어

#### 37. [난이도 중] 다음 중 '고립성'에 대한 설명으로 가장 적절한 것은?

1. 동시에 실행되는 트랜잭션이 서로 간섭하지 않도록 보장하는 성질
2. COMMIT된 트랜잭션의 결과가 장애 후에도 영구 보존되는 성질
3. 기존 객체의 구조를 변경(컬럼 추가·수정·삭제 등)하는 DDL 명령어
4. 행을 유일하게 식별하며 NULL과 중복을 허용하지 않는 제약조건
5. 부모 테이블의 PK 또는 UNIQUE 값을 참조하여 무결성을 보장하는 제약조건

#### 38. [난이도 상] "정규표현식에 일치하는 부분 문자열을 추출하는 함수"에 해당하는 개념은?

1. UNION ALL
2. REGEXP_SUBSTR
3. 인라인 뷰
4. IN 연산자
5. ALL

#### 39. [난이도 중] 다음 중 'NVL2'에 대한 설명으로 가장 적절한 것은?

1. Oracle에서 NULL 여부에 따라 두 가지 다른 값을 반환하는 함수
2. 두 표현식이 같으면 NULL, 다르면 첫 표현식을 반환하는 함수
3. Oracle 전용으로 값을 비교하여 해당 결과를 반환하는 조건 함수
4. 양쪽 테이블에서 조인 조건이 참인 행만 결과에 포함하는 조인
5. 양쪽 테이블 모두의 불일치 행을 보존하는 조인

#### 40. [난이도 중] 다음 중 '일관성'에 대한 설명으로 가장 적절한 것은?

1. 컬럼 값의 중복을 금지하지만 NULL은 허용하는 제약조건
2. 트랜잭션 전후로 데이터베이스가 무결성 규칙을 유지하는 성질
3. 동시에 실행되는 트랜잭션이 서로 간섭하지 않도록 보장하는 성질
4. 테이블·뷰·시퀀스 등 객체 구조를 새로 생성하는 DDL 명령어
5. 테이블 구조를 남기고 모든 행을 빠르게 제거하는 DDL 명령어

#### 41. [난이도 중] 다음 중 'SAVEPOINT'에 대한 설명으로 가장 적절한 것은?

1. 동시에 실행되는 트랜잭션이 서로 간섭하지 않도록 보장하는 성질
2. 테이블·뷰·시퀀스 등 객체 구조를 새로 생성하는 DDL 명령어
3. 테이블 구조를 남기고 모든 행을 빠르게 제거하는 DDL 명령어
4. 트랜잭션 내부에 부분 롤백 가능한 지점을 지정하는 TCL 명령어
5. 트랜잭션 작업이 모두 성공하거나 모두 실패해야 하는 성질

#### 42. [난이도 중] "해당 컬럼이 집계로 인한 NULL이면 1, 실제 데이터이면 0을 반환하는 함수"에 해당하는 개념은?

1. LAG
2. NTILE
3. CONNECT BY PRIOR
4. GROUPING 함수
5. RANK

#### 43. [난이도 중] "지정한 컬럼 순서에 따른 계층적 소계와 총계를 생성하는 그룹 확장"에 해당하는 개념은?

1. GROUPING SETS
2. ROW_NUMBER
3. LAG
4. FIRST_VALUE
5. ROLLUP

#### 44. [난이도 중] 다음 중 'INTERSECT'에 대한 설명으로 가장 적절한 것은?

1. 동점에 같은 순위를 주고 다음 순위를 연속으로 부여하는 윈도우 함수
2. 두 결과 집합의 교집합만 반환하는 집합 연산자
3. 지정한 컬럼 순서에 따른 계층적 소계와 총계를 생성하는 그룹 확장
4. 원하는 그룹 조합만 명시적으로 지정하여 집계하는 그룹 확장
5. 동점 여부와 무관하게 각 행에 고유한 순번을 부여하는 윈도우 함수

#### 45. [난이도 하] "두 결과 집합을 중복 제거 없이 그대로 결합하는 집합 연산자"에 해당하는 개념은?

1. GROUPING SETS
2. RANK
3. LAG
4. UNION ALL
5. ROLLUP

#### 46. [난이도 상] "테이블·뷰·시퀀스 등 객체 구조를 새로 생성하는 DDL 명령어"에 해당하는 개념은?

1. CREATE
2. TRUNCATE
3. UNIQUE
4. VIEW
5. WITH GRANT OPTION

#### 47. [난이도 상] "현재 행을 기준으로 이후 행의 값을 참조하는 윈도우 함수"에 해당하는 개념은?

1. CONNECT BY PRIOR
2. 스칼라 서브쿼리
3. 상관 서브쿼리
4. LEAD
5. NTILE

#### 48. [난이도 하] 다음 중 'INSERT'에 대한 설명으로 가장 적절한 것은?

1. 기존 행의 컬럼 값을 SET 절로 변경하는 DML 명령어
2. 매칭 여부에 따라 INSERT·UPDATE·DELETE를 조건부 수행하는 DML
3. 아직 확정하지 않은 트랜잭션의 변경을 취소하는 TCL 명령어
4. 트랜잭션 작업이 모두 성공하거나 모두 실패해야 하는 성질
5. 테이블에 새 행을 추가하는 DML 명령어

#### 49. [난이도 하] 다음 중 'WHERE'에 대한 설명으로 가장 적절한 것은?

1. GROUP BY로 만들어진 그룹에 조건을 적용하는 절
2. Oracle에서 NULL이면 지정한 대체값을 반환하는 함수
3. 두 표현식이 같으면 NULL, 다르면 첫 표현식을 반환하는 함수
4. Oracle 전용으로 값을 비교하여 해당 결과를 반환하는 조건 함수
5. GROUP BY보다 먼저 개별 행을 필터링하는 절

#### 50. [난이도 상] "두 테이블에서 이름이 같은 모든 컬럼을 자동으로 조인하는 방식"에 해당하는 개념은?

1. 비등가 조인
2. DDL
3. DCL
4. NATURAL JOIN
5. SELF JOIN

## 정답표

| 문항 | 정답 | 문항 | 정답 |
|---:|:---:|---:|:---:|
| 1 | 2 | 26 | 3 |
| 2 | 4 | 27 | 5 |
| 3 | 3 | 28 | 4 |
| 4 | 1 | 29 | 5 |
| 5 | 1 | 30 | 2 |
| 6 | 4 | 31 | 4 |
| 7 | 3 | 32 | 2 |
| 8 | 3 | 33 | 4 |
| 9 | 3 | 34 | 3 |
| 10 | 1 | 35 | 3 |
| 11 | 1 | 36 | 1 |
| 12 | 1 | 37 | 1 |
| 13 | 4 | 38 | 2 |
| 14 | 4 | 39 | 1 |
| 15 | 5 | 40 | 2 |
| 16 | 2 | 41 | 4 |
| 17 | 2 | 42 | 4 |
| 18 | 3 | 43 | 5 |
| 19 | 1 | 44 | 2 |
| 20 | 4 | 45 | 4 |
| 21 | 1 | 46 | 1 |
| 22 | 5 | 47 | 4 |
| 23 | 4 | 48 | 5 |
| 24 | 4 | 49 | 5 |
| 25 | 1 | 50 | 4 |

## 정답 및 해설

### 1. 2번

같은 조건으로 재조회 시 다른 트랜잭션의 삽입으로 새 행이 나타나는 현상. 커밋 안 된 데이터 읽기는 Dirty Read입니다.

### 2. 4번

두 개 이상의 엔터티 사이의 행위로 발생하는 엔터티. 주문상세, 이력 등이며 발생 시점이 가장 늦습니다.

### 3. 3번

정답은 단순화입니다. 단순화는 표기법의 약속, 명확화는 의미의 단일 해석입니다.

### 4. 1번

정답은 개념적 모델링입니다. DBMS 설계는 물리적 모델링입니다.

### 5. 1번

데이터 중복을 줄이고 이상현상을 방지하기 위해 엔터티를 분해하는 과정. 의도적 중복 허용은 반정규화입니다.

### 6. 4번

정답은 논리적 모델링입니다. 첫 단계는 개념적 모델링이고, 정규화가 수행되는 핵심 단계입니다.

### 7. 3번

인스턴스가 가지는 더 이상 분리할 수 없는 최소 의미 단위. 하나의 속성은 같은 종류의 값만 가지며 주식별자에 함수적으로 종속됩니다.

### 8. 3번

1:1, 1:M처럼 관계에 참여하는 인스턴스 수의 대응 비율. M:N 관계는 교차 엔터티를 두어 1:M + M:1로 분해합니다.

### 9. 3번

정답은 중심 엔터티입니다. 주문, 계약 등이며 데이터량이 많은 편입니다.

### 10. 1번

정답은 행위 엔터티입니다. 주문상세, 이력 등이며 발생 시점이 가장 늦습니다.

### 11. 1번

동점에 같은 순위를 주고 다음 순위를 건너뛰는 윈도우 함수. 1,1,3 방식이며, DENSE_RANK는 1,1,2 방식입니다.

### 12. 1번

정답은 CONNECT BY PRIOR입니다. START WITH는 루트 조건, LEVEL은 깊이를 나타냅니다.

### 13. 4번

Oracle 계층형 질의에서 부모와 자식의 연결 방향을 정의하는 절. START WITH는 루트 조건, LEVEL은 깊이를 나타냅니다.

### 14. 4번

현재 행을 기준으로 이전 행의 값을 참조하는 윈도우 함수. 이후 값은 LEAD입니다. LAG(col, n, default) 형태입니다.

### 15. 5번

Oracle에서 결과가 반환되는 과정에 임시로 부여되는 순번. 정렬 후 Top-N은 인라인 뷰나 FETCH FIRST를 사용합니다.

### 16. 2번

윈도우 프레임에서 정렬 순서상 첫 번째 값을 반환하는 함수. 마지막 값은 LAST_VALUE이며 프레임 명시가 필요합니다.

### 17. 2번

정답은 일관성입니다. 간섭 통제는 고립성(Isolation)입니다.

### 18. 3번

테이블에서 조건에 맞는 행을 제거하는 DML 명령어. DDL로 모든 행 제거는 TRUNCATE입니다. DELETE는 ROLLBACK 가능합니다.

### 19. 1번

정답은 WITH ADMIN OPTION입니다. 객체 권한 재부여는 WITH GRANT OPTION이며, ADMIN OPTION은 연쇄 회수되지 않습니다.

### 20. 4번

정답은 PIVOT입니다. 열→행은 UNPIVOT입니다.

### 21. 1번

정답은 HAVING입니다. 개별 행 필터는 WHERE입니다. 집계 함수를 조건에 사용할 수 있습니다.

### 22. 5번

SELECT, INSERT, UPDATE, DELETE처럼 행 데이터를 조작하는 언어. 객체 구조 정의는 DDL입니다.

### 23. 4번

GRANT, REVOKE처럼 권한을 부여하거나 회수하는 언어. 트랜잭션 제어는 TCL입니다.

### 24. 4번

양쪽 테이블 모두의 불일치 행을 보존하는 조인. Oracle의 (+) 기호로는 FULL OUTER JOIN을 표현할 수 없습니다.

### 25. 1번

정답은 FULL OUTER JOIN입니다. Oracle의 (+) 기호로는 FULL OUTER JOIN을 표현할 수 없습니다.

### 26. 3번

정답은 UNIQUE입니다. NULL+중복 모두 금지는 PRIMARY KEY입니다.

### 27. 5번

정답은 SELF JOIN입니다. 조직도의 상사-부하, 계층 구조 표현에 자주 사용됩니다.

### 28. 4번

정답은 ALTER입니다. 완전 제거는 DROP입니다.

### 29. 5번

COMMIT된 트랜잭션의 결과가 장애 후에도 영구 보존되는 성질. 무결성 유지는 일관성(Consistency)입니다.

### 30. 2번

동점 여부와 무관하게 각 행에 고유한 순번을 부여하는 윈도우 함수. 동점 건너뛰기는 RANK, 동점 연속은 DENSE_RANK입니다.

### 31. 4번

시스템 권한이나 ROLE을 다른 사용자에게 재부여할 수 있게 하는 옵션. 객체 권한 재부여는 WITH GRANT OPTION이며, ADMIN OPTION은 연쇄 회수되지 않습니다.

### 32. 2번

조건에 따라 값을 선택하며 ELSE가 없고 모든 조건이 거짓이면 NULL을 반환하는 표현식. Oracle 전용 조건 함수는 DECODE입니다.

### 33. 4번

정답은 DELETE입니다. DDL로 모든 행 제거는 TRUNCATE입니다. DELETE는 ROLLBACK 가능합니다.

### 34. 3번

정답은 INSERT입니다. 값 변경은 UPDATE, 행 삭제는 DELETE입니다.

### 35. 3번

정답은 DENSE_RANK입니다. 1,1,2 방식이며, 고유 순번은 ROW_NUMBER입니다.

### 36. 1번

현재 트랜잭션의 변경을 영구적으로 확정하는 TCL 명령어. 취소는 ROLLBACK입니다.

### 37. 1번

동시에 실행되는 트랜잭션이 서로 간섭하지 않도록 보장하는 성질. 전부/전무는 원자성(Atomicity)입니다.

### 38. 2번

정답은 REGEXP_SUBSTR입니다. 개수는 REGEXP_COUNT, 위치는 REGEXP_INSTR입니다.

### 39. 1번

Oracle에서 NULL 여부에 따라 두 가지 다른 값을 반환하는 함수. NVL2(expr, NULL아닐때값, NULL일때값)입니다.

### 40. 2번

트랜잭션 전후로 데이터베이스가 무결성 규칙을 유지하는 성질. 간섭 통제는 고립성(Isolation)입니다.

### 41. 4번

트랜잭션 내부에 부분 롤백 가능한 지점을 지정하는 TCL 명령어. ROLLBACK TO로 해당 지점까지 되돌릴 수 있습니다.

### 42. 4번

정답은 GROUPING 함수입니다. NULL 치환은 NVL/COALESCE입니다.

### 43. 5번

정답은 ROLLUP입니다. 모든 조합은 CUBE입니다. ROLLUP(A,B)은 (A,B),(A),() 세 수준입니다.

### 44. 2번

두 결과 집합의 교집합만 반환하는 집합 연산자. 차집합은 MINUS(Oracle)/EXCEPT(SQL Server)입니다.

### 45. 4번

정답은 UNION ALL입니다. 중복 제거 비용이 없어 UNION보다 성능이 우수합니다.

### 46. 1번

정답은 CREATE입니다. 구조 변경은 ALTER입니다.

### 47. 4번

정답은 LEAD입니다. 이전 값은 LAG입니다.

### 48. 5번

테이블에 새 행을 추가하는 DML 명령어. 값 변경은 UPDATE, 행 삭제는 DELETE입니다.

### 49. 5번

GROUP BY보다 먼저 개별 행을 필터링하는 절. 그룹 조건은 HAVING을 사용합니다.

### 50. 4번

정답은 NATURAL JOIN입니다. ON/USING과 함께 사용 불가이며, 동일 이름 컬럼 변화에 민감합니다.

## 이 세트의 참고 출처

- [이패스코리아 제57회 기출 복원](https://www.epasskorea.com/board/restore_exam.asp?Cate_IDX=&LEC_IDX=&TEA_IDX=&bcs_idx=&bg_idx=61&bmode=detail&intB_IDX=40&intBc_idx=442&link_idx=10784&page=1&pageb=&searchoption=B.TITLE&searchstring=sqld)
- [문어CBT 제59회 기출 복원](https://www.sqldpass.com/blog/past-exam/sqld-2025-59)
- [K-DATA 데이터자격시험](https://www.dataq.or.kr/)

